[build]
builder = "NIXPACKS"
buildCommand = "cd booking-system && npm install"

[deploy]
startCommand = "cd booking-system && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
